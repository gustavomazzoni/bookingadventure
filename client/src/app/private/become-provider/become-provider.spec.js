describe( 'become-provider section', function() {
  beforeEach( module( 'bookingadventure.become-provider' ) );

  it( 'should have a dummy test', inject( function() {
    expect( true ).toBeTruthy();
  }));
});

